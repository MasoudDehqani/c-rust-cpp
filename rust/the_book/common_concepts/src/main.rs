/*
    - values (variable) which are bind to a name or identifier are
    by default immutable in Rust
    - immutability contributes to safety and easy concurrency
*/

fn main() {
    println!("Hello, world!");
}
